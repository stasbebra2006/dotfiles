# tmux reads this entry-point file automatically when a new server starts.
# Each source-file command executes one ordinary config file in this order.
source-file ~/.config/tmux/general.conf
source-file ~/.config/tmux/keybindings.conf
source-file ~/.config/tmux/passthrough.conf
source-file ~/.config/tmux/appearance.conf
